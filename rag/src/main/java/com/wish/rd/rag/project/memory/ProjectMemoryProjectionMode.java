package com.wish.rd.rag.project.memory;

/**
 * Future optional OpenViking detail projection. This change freezes the SPI at {@link #OFF};
 * PostgreSQL remains the only production retrieval and canonical head path.
 */
public enum ProjectMemoryProjectionMode {
    OFF
}
