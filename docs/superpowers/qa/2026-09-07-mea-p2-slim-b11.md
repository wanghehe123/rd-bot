# MEA B11 slim P2 live (2026-09-07)

## Scope

Authorized commit + **slim** B11 live probes after deploy. Do **not** run 12×3; do **not** claim full P2-W1/W2/W3 matrix or R0 / §6.4.

Primary assert: episode budget freeze (`FROZEN_FROM_ENV`) on a dispatched role snapshot.

## Deploy lineage

| Item | Value |
| --- | --- |
| Branch | `codex/mea-fresh-executor-episode` |
| Tip (code) | `db51d1c5` feat(mea): close B09–B11 recovery gaps, evidence trust, and episode budget freeze |
| Jar SHA-256 | `a4bcc66fd50932150865c1489308af374b24c97953f099441ec1c087ad7cf8b6` |
| Budget env | `RD_PI_MAX_AGENT_TURNS=40`, `RD_PI_MAX_TOTAL_TOKENS=2000000` via `deploy/cloud-server/episode-budget.sh` |
| Host | `106.55.13.166` |

## Slim task

| Field | Value |
| --- | --- |
| Output | `/tmp/mea-p2-slim-20260907` |
| taskId | `7502657531727187968` |
| Title | MEA-P2-SLIM 首页标记 |
| Evidence role | `REQUIREMENT_REVIEWER` stageRunId `7502657538601652224` (coding still PENDING when budget captured) |

## Probes

| Probe | Status | Evidence |
| --- | --- | --- |
| P2-BUDGET | **PASS** | snapshot + `input/request.json`: `maxAgentTurns=40`, `maxTotalTokens=2000000`, `episodeBudgetStatus=FROZEN_FROM_ENV` |
| P2-ISO | **SKIPPED** | QA `provider-attempts/` not observed in slim window (unit + archived W3 still cover isolation) |
| P2-GAP | **SKIPPED** | No `HOST_VERIFY_FIX` / audited-gap surface in this slim run (B09 covered by marker unit tests) |

## Commands

```bash
# cloud: source episode-budget.env via start-backend.sh, then
RD_BOT_BASE_URL=http://127.0.0.1:8080 MEA_EVAL_PROJECT_ID=7499721648870920192 \
  python3 deploy/cloud-server/mea-live/verify_p2_slim.py --output /tmp/mea-p2-slim

curl -s "http://127.0.0.1:8080/admin/rd-tasks/<taskId>/stage-runs/<stageRunId>/execution-profile"
```

## Honest closeout

- B11 live budget freeze: **done (slim)**.
- Full P2-W1/W2/W3 + BUDGET_EXCEEDED→Host blocker assert: **still open**.
- Not R0; not 12×3.
