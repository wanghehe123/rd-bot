package com.wish.rd.engine.evaluation.model;

/** Declares whether a dataset proves scorer behavior or product quality. */
public enum EvaluationDatasetKind {
    SCORER_SMOKE,
    QUALITY_BENCHMARK,
    TASK_RUN
}
