package com.wish.rd.engine.requirement.audit;

/** Deterministic contract-audit verdict. {@code NEEDS_REVISION} / {@code INVALID} are reserved for a later change. */
public enum ContractAuditVerdict {
    ALIGNED,
    UNKNOWN
}
