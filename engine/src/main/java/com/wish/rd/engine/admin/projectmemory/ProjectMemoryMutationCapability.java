package com.wish.rd.engine.admin.projectmemory;

/** Host-injected governance capabilities; request body actors are never trusted. */
public enum ProjectMemoryMutationCapability {
    PROJECT_MEMORY_GOVERN,
    PROJECT_MEMORY_PURGE
}
