# RD-Bot 研发交付智能体平台

**技术栈**：Java 21 + Spring Boot 3.5 + PostgreSQL + MyBatis-Plus + Redis/Redisson + RocketMQ + Docker + GitHub API + 飞书 OpenAPI + React/Vite

**项目简介**：RD-Bot 是面向研发交付场景的多智能体 Harness 平台，围绕需求/工单到 PR 的交付链路，通过四阶段流水线编排大模型完成代码生成任务，支持中断恢复、沙箱验证和模型降级。

**上下文工程**：针对大模型上下文窗口有限、输入噪声多的问题，设计角色上下文包，将需求材料、检索证据、验收标准、风险提示按需求评审/方案设计/编码/QA 四个角色分级裁剪分发，避免一次性塞入全部资料导致模型注意力分散。

**多智能体任务编排**：将复杂研发任务拆解为需求评审→方案设计→编码执行→QA 验证四阶段流水线，每阶段定义独立输入输出协议与终止条件，阶段产物持久化落库支持断点续跑；相比单次直出策略，交付完整率（补丁+回归测试+验收证据三件套）从 **0%（单次直出 0/4）** 提升至 **100%（四阶段流水线 4/4）**，阶段失败时可精准定位至具体环节。

**工具与技能治理**：设计 Skill 注册表与安装编排引擎，通过角色白名单、版本匹配、元数据完整性校验控制各阶段 Agent 可加载的 Skill；按风险等级（LOW/MEDIUM/HIGH）分级管控，高风险 Skill 触发人工审批；在各角色 Prompt 中约束操作边界（是否允许改代码、创建 PR、必须输出的 JSON 字段），降低工具误用风险。

**状态与记忆管理**：设计「任务主状态 + 阶段运行记录」双层状态模型，将上下文、执行产物、失败原因持久化至 PostgreSQL，解决大模型无状态导致中断后丢失进度的问题；在 **【待填写：】** 次强制中断实验中，断点续跑成功率达 **【待填写：%】**，已成功阶段产物零重跑；而 Claude Code 等无状态会话工具中断后只能从头重来。

**沙箱测试与结果评估**：QA 阶段构建 Docker 隔离环境，后端接口执行自动化测试覆盖，前端页面渲染、交互链路、跨浏览器兼容性由 Playwright 截图比对与断言校验，采集截图证据、网络请求、控制台日志生成结构化验收报告。引入端到端验证后，在 15 个受控注入缺陷（后端逻辑 6 + 前端 9）上，QA 阶段问题检出率从仅依赖单元测试的 **60%（9/15）** 提升至 **100%（15/15，单测+Playwright 浏览器验证）**，前端渲染类缺陷漏检率降低 **67 个百分点（67%→0%）**。

**约束与容错恢复**：在策略门控、需求评审、QA 复核、PR 发布等关键节点设置拦截机制，对需求信息不足、越权、执行异常、阶段失败等场景进行错误分类和状态落库；支持模型供应商自动降级（GPT-4 → Claude → 国产模型）与飞书告警通知。

| 基准测试 | 规模 | 成功率 | 平均耗时 |
|---------|------|--------|----------|
| SWE-bench Lite 配对子集（vs Claude Code） | 4 题（django Lite pilot） | resolved 3/4（75%） vs 0/4（0%） | RD 33.8 vs CC 10.2 |
| 自建 Java 场景（Mini Jira） | **【待填写：】** 题 | **【待填写：%】** | **【待填写：分钟】** |
| 中断恢复测试（vs Claude Code 无状态模式） | **【待填写：】** 次 | **【待填写：%】** vs 0%（无状态） | — |
