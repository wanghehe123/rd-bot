# Optional Pi episode budget freeze for live B11 probes (sourced by start-backend.sh).
# Positive values → snapshot episodeBudgetStatus=FROZEN_FROM_ENV; omit file for NOT_FROZEN.
export RD_PI_MAX_AGENT_TURNS=40
export RD_PI_MAX_TOTAL_TOKENS=2000000
